{
  "name": "seatseller-worker",
  "version": "1.0.0",
  "description": "Persistent Playwright-stealth worker that scrapes SeatSeller B2B (in3.seatseller.travel) and exposes a small HTTP API for the main Cheap Travels backend to call. Run on a small VPS, NOT on Vercel/serverless.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "install:chromium": "playwright install chromium --with-deps"
  },
  "dependencies": {
    "express": "^4.21.2",
    "playwright": "^1.48.0",
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
